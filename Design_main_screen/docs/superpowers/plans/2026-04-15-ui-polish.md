# UI 폴리싱 및 세부 개선 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** motion 라이브러리로 각 화면에 spring 기반 애니메이션, 빈 상태(Empty State), shimmer를 추가해 앱 완성도를 높인다.

**Architecture:** 공통 토큰 파일(`animationTokens.ts`)을 먼저 만들고, BottomNav → HomeScreen/ProfileScreen → ExploreTab → ChatTab → ProfileTab 순서로 각 파일을 최소 수정. shimmer는 `src/styles/index.css`에 keyframe 추가.

**Tech Stack:** motion (framer-motion v12, `"motion/react"` import), React 18 + TypeScript, CSS keyframes

---

## 파일 구조

| 상태 | 경로 | 역할 |
|------|------|------|
| Create | `src/app/components/animationTokens.ts` | 공통 spring/stagger 상수 |
| Modify | `src/styles/index.css` | shimmer keyframe + `.avatar-shimmer` 클래스 추가 |
| Modify | `src/app/components/BottomNav.tsx` | motion.button, layoutId dot, icon bounce |
| Modify | `src/app/components/HomeScreen.tsx` | AnimatePresence(ProfileScreen), 카드 stagger |
| Modify | `src/app/components/CharacterCard.tsx` | motion.div whileTap/whileHover |
| Modify | `src/app/components/ProfileScreen.tsx` | 슬라이드업, 관심 버튼 spring bounce |
| Modify | `src/app/components/ExploreTabScreen.tsx` | 검색 오버레이 fade+scale, 결과 stagger, 칩 whileTap |
| Modify | `src/app/components/ChatTabScreen.tsx` | FADE_UP, 목록 stagger, Empty State |
| Modify | `src/app/components/ProfileTabScreen.tsx` | 그리드 stagger, shimmer, Empty State 애니메이션 |

---

## Chunk 1: 공통 기반

### Task 1: animationTokens.ts 생성

**Files:**
- Create: `src/app/components/animationTokens.ts`

- [ ] **Step 1: 파일 생성**

```ts
// src/app/components/animationTokens.ts

export const SPRING = { type: "spring" as const, stiffness: 300, damping: 24 };

export const FADE_UP = {
  initial: { opacity: 0, y: 20 },
  animate: { opacity: 1, y: 0 },
  exit: { opacity: 0, y: 10 },
};

// initial: {} 필수 — 없으면 자식 variant가 전파되지 않음
export const STAGGER_CONTAINER = {
  initial: {},
  animate: { transition: { staggerChildren: 0.06 } },
};

export const STAGGER_ITEM = {
  initial: { opacity: 0, y: 16 },
  animate: { opacity: 1, y: 0, transition: { ...SPRING } },
};
```

- [ ] **Step 2: 빌드 확인**

```bash
cd Design_main_screen && npm run build 2>&1 | tail -5
```
Expected: `✓ built in` (오류 없음)

- [ ] **Step 3: 커밋**

```bash
git add src/app/components/animationTokens.ts
git commit -m "feat: add animation tokens (spring, stagger, fade-up)"
```

---

### Task 2: shimmer CSS keyframe 추가

**Files:**
- Modify: `src/styles/index.css`

- [ ] **Step 1: index.css 맨 아래에 추가**

기존 파일 내용:
```css
@import './fonts.css';
@import './tailwind.css';
@import './theme.css';
```

아래 내용을 파일 끝에 추가:
```css
/* ── Shimmer skeleton ── */
@keyframes shimmer {
  0%   { background-position: -200% 0; }
  100% { background-position:  200% 0; }
}
.avatar-shimmer {
  background: linear-gradient(90deg, #1e2d40 25%, #2a3d52 50%, #1e2d40 75%);
  background-size: 200% 100%;
  animation: shimmer 1.4s infinite linear;
}
```

- [ ] **Step 2: 빌드 확인**

```bash
cd Design_main_screen && npm run build 2>&1 | tail -5
```
Expected: `✓ built in`

- [ ] **Step 3: 커밋**

```bash
git add src/styles/index.css
git commit -m "feat: add avatar shimmer keyframe"
```

---

## Chunk 2: BottomNav + ProfileScreen

### Task 3: BottomNav — icon bounce + layoutId indicator

**Files:**
- Modify: `src/app/components/BottomNav.tsx`

현재 구조: `navItems.map` 내부에 `<button>` → 내부에 아이콘 `<div>` + label `<span>`.

- [ ] **Step 1: import 추가 및 button → motion.button 교체**

파일 상단 import를 아래로 교체:
```tsx
import { motion } from "motion/react";
import { Home, Compass, MessageCircle, User } from "lucide-react";
import { SPRING } from "./animationTokens";
```

- [ ] **Step 2: map 내부 button → motion.button + dot 추가**

BottomNav.tsx의 `.map()` 콜백은 화살표 함수 묵시적 반환(`=> ( ... )`) 형태이므로 `return` 키워드를 추가하지 않는다.
기존 `<button key={item.id} ...> ... </button>` JSX 블록 전체(return 키워드 없음)를 아래로 교체:

```tsx
<motion.button
  key={item.id}
  onClick={() => setActiveNav(item.id)}
  whileTap={{ scale: 0.85 }}
  className="flex flex-col items-center gap-1 min-w-[44px]"
>
  <div
    className="relative w-9 h-9 rounded-xl flex items-center justify-center transition-all"
    style={{ background: isActive ? "rgba(14,165,233,0.12)" : "transparent" }}
  >
    <motion.span
      key={isActive ? `${item.id}-active` : item.id}
      animate={isActive ? { scale: [1, 1.25, 1] } : { scale: 1 }}
      transition={{ ...SPRING }}
      style={{ display: "flex" }}
    >
      <Icon
        size={20}
        color={isActive ? "#0EA5E9" : "#94A3B8"}
        strokeWidth={isActive ? 2.5 : 1.8}
      />
    </motion.span>
  </div>
  <span
    style={{
      fontSize: "9px",
      color: isActive ? "#0EA5E9" : "#94A3B8",
      fontWeight: isActive ? 700 : 400,
    }}
  >
    {item.label}
  </span>
  {/*
    layoutId="nav-indicator" — 4개 탭 모두에 동일한 layoutId dot 렌더링.
    비활성 탭은 opacity:0, 활성 탭은 opacity:1. framer-motion이 슬라이딩 보간.
    주의: dot에 key 추가 금지 — layoutId 연결이 끊김.
  */}
  <motion.div
    layoutId="nav-indicator"
    style={{
      width: 4,
      height: 4,
      borderRadius: "50%",
      background: "#0EA5E9",
      opacity: isActive ? 1 : 0,
      marginTop: -2,
    }}
  />
</motion.button>
```

- [ ] **Step 3: 빌드 확인**

```bash
cd Design_main_screen && npm run build 2>&1 | tail -5
```
Expected: `✓ built in`

- [ ] **Step 4: 커밋**

```bash
git add src/app/components/BottomNav.tsx
git commit -m "feat(BottomNav): spring icon bounce + layoutId indicator slide"
```

---

### Task 4: ProfileScreen — 슬라이드업 진입 + 관심 버튼 spring bounce

**Files:**
- Modify: `src/app/components/ProfileScreen.tsx`

ProfileScreen의 최상위 컨테이너는 `position: fixed, inset: 0`인 `<div>`. 이것을 `motion.div`로 교체하고 슬라이드업 animation을 추가한다. AnimatePresence는 HomeScreen.tsx에서 처리하므로 여기서는 motion.div만 교체.

관심 버튼은 현재 `isInterested` prop을 받아 스타일을 분기하는 `<button>`이다. 이를 `motion.button`으로 교체한다.

- [ ] **Step 1: import 추가**

ProfileScreen.tsx 상단의 기존 import 블록에 아래 두 줄 추가:
```tsx
import { motion } from "motion/react";
import { SPRING } from "./animationTokens";
```

- [ ] **Step 2: 최상위 컨테이너 div → motion.div 교체**

ProfileScreen의 최상위 렌더 요소(`position: "fixed", inset: 0, background: "#0B1526", zIndex: 30` 스타일을 가진 div)를 아래로 교체:

```tsx
<motion.div
  initial={{ y: "100%" }}
  animate={{ y: 0 }}
  exit={{ y: "100%" }}
  transition={{ type: "spring", stiffness: 260, damping: 28 }}
  style={{
    position: "fixed",
    inset: 0,
    background: "#0B1526",
    zIndex: 30,
    overflowY: "auto",
  }}
>
```
닫는 태그도 `</motion.div>`로 변경.

- [ ] **Step 3: 관심 버튼 motion.button으로 교체**

ProfileScreen.tsx:532-550의 관심 버튼을 찾는다. 이 버튼은 로컬 state `isFollowing`으로 스타일을 분기한다.
해당 `<button>` 을 아래 `<motion.button>`으로 교체한다.
`animate`의 조건은 반드시 `isFollowing`(로컬 state)을 사용한다 — prop인 `isInterested`가 아님.
`onClick`은 기존 로직 (`setIsFollowing(next)` + `onToggleInterest?.()` + toast) 을 그대로 보존한다:

```tsx
<motion.button
  whileTap={{ scale: 0.88 }}
  animate={isFollowing ? { scale: [1, 1.3, 1] } : { scale: 1 }}
  transition={{ ...SPRING }}
  onClick={() => {
    const next = !isFollowing;
    setIsFollowing(next);
    onToggleInterest?.();
    toast(next ? `${character.name} 님을 관심 추가했어요 ♥` : `${character.name} 님의 관심을 해제했어요`);
  }}
  className="flex-1 py-2.5 rounded-2xl text-sm font-bold transition-all"
  style={{
    background: isFollowing
      ? "white"
      : `linear-gradient(135deg, ${character.accentColor}cc, ${character.accentColor})`,
    color: isFollowing ? character.accentColor : "white",
    border: isFollowing ? `2px solid ${character.accentColor}` : "none",
    boxShadow: isFollowing ? "none" : `0 6px 18px ${character.accentColor}44`,
  }}
>
  {isFollowing ? "관심중 ✓" : "관심 추가"}
</motion.button>
```

- [ ] **Step 4: 빌드 확인**

```bash
cd Design_main_screen && npm run build 2>&1 | tail -5
```
Expected: `✓ built in`

- [ ] **Step 5: 커밋**

```bash
git add src/app/components/ProfileScreen.tsx
git commit -m "feat(ProfileScreen): slide-up entry + interest button spring bounce"
```

---

### Task 5: HomeScreen — AnimatePresence(ProfileScreen) + 카드 stagger

**Files:**
- Modify: `src/app/components/HomeScreen.tsx`
- Modify: `src/app/components/CharacterCard.tsx`

**HomeScreen.tsx 수정:**

- [ ] **Step 1: import 추가**

HomeScreen.tsx 상단 import 블록에 추가:
```tsx
import { AnimatePresence, motion } from "motion/react";
import { STAGGER_CONTAINER, STAGGER_ITEM, FADE_UP } from "./animationTokens";
```

- [ ] **Step 2: ProfileScreen 조건부 렌더링을 AnimatePresence로 감싸기**

현재 코드 (HomeScreen.tsx:262-275):
```tsx
{!chatCharacter && selectedCharacter && (
  <ProfileScreen
    character={selectedCharacter}
    ...
  />
)}
```

아래로 교체:
```tsx
<AnimatePresence>
  {!chatCharacter && selectedCharacter && (
    <ProfileScreen
      key={selectedCharacter.id}
      character={selectedCharacter}
      onBack={() => setSelectedCharacter(null)}
      onChatClick={() => setChatCharacter(selectedCharacter)}
      hasStory={hasCharacterStory(selectedCharacter.id)}
      onViewStory={() => {
        const stories = buildStories([selectedCharacter]);
        setStoryState({ index: 0, stories });
      }}
      isInterested={interestIds.includes(selectedCharacter.id)}
      onToggleInterest={() => toggleInterest(selectedCharacter.id)}
    />
  )}
</AnimatePresence>
```

- [ ] **Step 3: 홈 피드 카드 목록에 stagger 적용**

HomeScreen.tsx에서 캐릭터 카드 목록 컨테이너 (현재 `<div className="flex flex-col gap-4 ...">`)를 아래로 교체:

```tsx
<motion.div
  className="flex flex-col gap-4 md:grid md:grid-cols-2 xl:grid-cols-2 2xl:grid-cols-3"
  variants={STAGGER_CONTAINER}
  initial="initial"
  animate="animate"
>
  {characters.map((char) => (
    <motion.div key={char.id} variants={STAGGER_ITEM}>
      <CharacterCard
        character={char}
        onProfileClick={() => setSelectedCharacter(char)}
        onChatClick={() => setChatCharacter(char)}
      />
    </motion.div>
  ))}
</motion.div>
```

**CharacterCard.tsx 수정:**

- [ ] **Step 4: CharacterCard 최상위 div → motion.div + whileTap**

주의: HomeScreen.tsx Step 3에서 카드를 감싸는 `<motion.div variants={STAGGER_ITEM}>`가 이미 stagger용 외부 래퍼로 존재한다.
CharacterCard 자신의 루트에 `whileTap`/`whileHover`를 추가하면 transform이 겹쳐 hover 시 scale이 `1.02 × 1.02 ≈ 1.04`가 된다.
이 중첩은 허용 범위 내이나, **whileHover는 CharacterCard에만 두고 외부 stagger 래퍼에는 추가하지 않는다.**

CharacterCard.tsx import에 추가:
```tsx
import { motion } from "motion/react";
```

최상위 `<div className="relative overflow-hidden rounded-[24px] ...">`(라인 38-46)를 아래로 교체:
```tsx
<motion.div
  className="relative overflow-hidden rounded-[24px] w-full max-w-[440px] mx-auto"
  whileTap={{ scale: 0.97 }}
  whileHover={{ scale: 1.02 }}
  transition={{ type: "spring", stiffness: 300, damping: 24 }}
  style={{
    height: "clamp(260px, calc(100vw - 32px), 380px)",
    border: "0.848px solid rgba(255,255,255,0.7)",
    boxShadow: "0px 4px 24px 0px rgba(14,165,233,0.14)",
  }}
>
```
닫는 태그도 `</motion.div>`로 변경.

- [ ] **Step 5: 빌드 확인**

```bash
cd Design_main_screen && npm run build 2>&1 | tail -5
```
Expected: `✓ built in`

- [ ] **Step 6: 커밋**

```bash
git add src/app/components/HomeScreen.tsx src/app/components/CharacterCard.tsx
git commit -m "feat(HomeScreen): AnimatePresence for ProfileScreen + card stagger + CharacterCard whileTap"
```

---

## Chunk 3: Explore + Chat + ProfileTab

### Task 6: ExploreTabScreen — 검색 오버레이 fade+scale + stagger

**Files:**
- Modify: `src/app/components/ExploreTabScreen.tsx`

ExploreTabScreen에는 `isSearching` state로 토글되는 SearchOverlay 컴포넌트가 있다.

- [ ] **Step 1: import 추가**

ExploreTabScreen.tsx 상단에 추가:
```tsx
import { AnimatePresence, motion } from "motion/react";
import { STAGGER_CONTAINER, STAGGER_ITEM } from "./animationTokens";
```

- [ ] **Step 2: SearchOverlay 최상위 div → motion.div + AnimatePresence 래핑**

SearchOverlay 컴포넌트의 최상위 `<div style={{ position:"fixed", inset:0, ... }}>` 를 아래로 교체:

```tsx
<motion.div
  initial={{ opacity: 0, scale: 0.97 }}
  animate={{ opacity: 1, scale: 1 }}
  exit={{ opacity: 0, scale: 0.97 }}
  transition={{ duration: 0.2 }}
  style={{ position: "fixed", inset: 0, background: "#0B1526", zIndex: 50, display: "flex", flexDirection: "column" }}
>
```

SearchOverlay를 호출하는 쪽 (`isSearching && <SearchOverlay .../>`)을 `<AnimatePresence>`로 감싼다.
**props는 기존 코드를 그대로 유지한다 — 변경 금지.** 아래는 구조만 보여주는 예시이며 코드 블록을 그대로 복사하지 말 것:

```
// 예시 (그대로 복사 금지)
<AnimatePresence>
  {isSearching && <SearchOverlay  /* 기존 props 그대로 */ />}
</AnimatePresence>
```

실제 구현 시에는 기존 `{isSearching && <SearchOverlay query={...} onQueryChange={...} ... />}` 전체를 `<AnimatePresence>` 태그로 감싸기만 한다.

- [ ] **Step 3: 검색 결과 카드 목록에 stagger 적용**

SearchOverlay 내부의 검색 결과 목록 컨테이너 `<div style={{ overflowY:"auto", ... }}>` (결과 카드들을 담는 div)를 아래로 교체:

```tsx
<motion.div
  style={{ overflowY: "auto", flex: 1 }}
  variants={STAGGER_CONTAINER}
  initial="initial"
  animate="animate"
>
  {results.map((char) => (
    <motion.div key={char.id} variants={STAGGER_ITEM}>
      {/* 기존 결과 카드 내용 */}
    </motion.div>
  ))}
</motion.div>
```

- [ ] **Step 4: 카테고리 칩 button → motion.button + whileTap**

카테고리 칩 버튼들 (`CATEGORIES.map(...)`) 내부의 `<button>` 을 `<motion.button whileTap={{ scale: 0.88 }}>` 으로 교체.

- [ ] **Step 5: 트렌딩 리스트 아이템 stagger 적용**

트렌딩 목록 컨테이너를 `STAGGER_CONTAINER`, 각 아이템을 `STAGGER_ITEM` variants로 감싸기.

- [ ] **Step 6: 빌드 확인**

```bash
cd Design_main_screen && npm run build 2>&1 | tail -5
```
Expected: `✓ built in`

- [ ] **Step 7: 커밋**

```bash
git add src/app/components/ExploreTabScreen.tsx
git commit -m "feat(Explore): search overlay fade+scale + results stagger + chip whileTap"
```

---

### Task 7: ChatTabScreen — FADE_UP + stagger + Empty State

**Files:**
- Modify: `src/app/components/ChatTabScreen.tsx`

- [ ] **Step 1: import 추가**

```tsx
import { motion } from "motion/react";
import { FADE_UP, STAGGER_CONTAINER, STAGGER_ITEM } from "./animationTokens";
```

- [ ] **Step 2: 최상위 div → motion.div + FADE_UP**

ChatTabScreen의 최상위 `<div style={{ position:"fixed", inset:0, ... }}>` 를 아래로 교체:
```tsx
<motion.div
  initial={FADE_UP.initial}
  animate={FADE_UP.animate}
  transition={{ duration: 0.25 }}
  style={{ position: "fixed", inset: 0, background: "#0B1526", display: "flex", flexDirection: "column", zIndex: 20, paddingBottom: "80px" }}
>
```

- [ ] **Step 3: 대화 목록 stagger 적용**

`ConversationList` 컴포넌트 내부의 `<div style={{ flex:1, overflowY:"auto" }}>` 를 아래로 교체:
```tsx
<motion.div
  style={{ flex: 1, overflowY: "auto" }}
  variants={STAGGER_CONTAINER}
  initial="initial"
  animate="animate"
>
  {conversations.map((conv) => (
    <motion.div key={conv.id} variants={STAGGER_ITEM}>
      <button onClick={() => onSelect(conv.character)} style={{ /* 기존 스타일 그대로 */ }}>
        {/* 기존 내용 그대로 */}
      </button>
    </motion.div>
  ))}
</motion.div>
```

- [ ] **Step 4: Empty State 추가**

`ConversationList`에서 `conversations.length === 0`일 때 표시할 Empty State (`FADE_UP` 토큰 사용):
```tsx
if (conversations.length === 0) {
  return (
    <motion.div
      initial={FADE_UP.initial}
      animate={FADE_UP.animate}
      transition={{ duration: 0.3 }}
      style={{ flex: 1, display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", padding: "48px 16px" }}
    >
      <div style={{ fontSize: 32, marginBottom: 12 }}>💬</div>
      <div style={{ color: "rgba(255,255,255,0.4)", fontSize: 13, marginBottom: 4 }}>아직 대화가 없어요</div>
      <div style={{ color: "#0EA5E9", fontSize: 12 }}>탐색에서 캐릭터를 찾아보세요 →</div>
    </motion.div>
  );
}
```

- [ ] **Step 5: 빌드 확인**

```bash
cd Design_main_screen && npm run build 2>&1 | tail -5
```
Expected: `✓ built in`

- [ ] **Step 6: 커밋**

```bash
git add src/app/components/ChatTabScreen.tsx
git commit -m "feat(Chat): FADE_UP entry + conversation stagger + empty state"
```

---

### Task 8: ProfileTabScreen — 그리드 stagger + shimmer + Empty State 애니메이션

**Files:**
- Modify: `src/app/components/ProfileTabScreen.tsx`

- [ ] **Step 1: import 추가**

```tsx
import { useState, useRef } from "react"; // ProfileTabScreen.tsx는 현재 useRef만 import 중 — useState 반드시 추가
import { motion } from "motion/react";
import { STAGGER_CONTAINER, STAGGER_ITEM, FADE_UP } from "./animationTokens";
```

- [ ] **Step 2 & 3: 그리드 stagger + shimmer 적용 (두 단계를 하나로 처리)**

`FollowedCharactersGrid` 내부를 아래와 같이 교체한다.
기존의 `characters.map(...)` 블록 전체(그리드 div 포함)를 아래로 대체:
- Step 2와 Step 3을 분리하면 `<button>` 중첩이 발생하므로 **반드시 한 번에 처리**한다.
- `CharacterGridCard`를 먼저 정의한 뒤 그리드에서 호출한다.

먼저 `FollowedCharactersGrid` 함수 **위에** 아래 헬퍼 컴포넌트를 추가:

```tsx
// FollowedCharactersGrid 정의 바로 위에 추가
function CharacterGridCard({ char, onSelect }: { char: Character; onSelect: (c: Character) => void }) {
  const [loaded, setLoaded] = useState(false);
  return (
    <button onClick={() => onSelect(char)} style={{ background: "none", border: "none", padding: 0, cursor: "pointer", width: "100%" }}>
      <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: "4px" }}>
        <div
          style={{
            width: "100%",
            aspectRatio: "1",
            borderRadius: "12px",
            background: `linear-gradient(135deg, ${char.accentColor ?? "#38BDF8"}, #0369A1)`,
            overflow: "hidden",
            position: "relative",
          }}
        >
          {!loaded && <div className="avatar-shimmer" style={{ position: "absolute", inset: 0, borderRadius: "12px" }} />}
          {char.avatarImage && (
            <img
              src={char.avatarImage}
              alt={char.name}
              onLoad={() => setLoaded(true)}
              style={{ width: "100%", height: "100%", objectFit: "cover", opacity: loaded ? 1 : 0 }}
            />
          )}
          <div style={{ position:"absolute", bottom:"3px", right:"3px", width:"8px", height:"8px", borderRadius:"50%", background:"#4ADE80", border:"1.5px solid #0B1526" }} />
        </div>
        <span style={{ color:"rgba(255,255,255,0.7)", fontSize:"8px", fontWeight:600, maxWidth:"100%", overflow:"hidden", textOverflow:"ellipsis", whiteSpace:"nowrap" }}>
          {char.name.split(" ").slice(-1)[0]}
        </span>
      </div>
    </button>
  );
}
```

그다음 `FollowedCharactersGrid` 내부의 그리드 `<div style={{ display:"grid", ... }}>` 블록 전체를 아래로 교체:

```tsx
<motion.div
  style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: "8px" }}
  variants={STAGGER_CONTAINER}
  initial="initial"
  animate="animate"
>
  {characters.map((char) => (
    <motion.div key={char.id} variants={STAGGER_ITEM}>
      <CharacterGridCard char={char} onSelect={onSelect} />
    </motion.div>
  ))}
</motion.div>
```

- [ ] **Step 4: 아바타 이미지에 shimmer 적용 (Step 2&3에서 완료)**

이 단계는 Step 2&3에서 이미 처리됐다. 건너뜀.

- [ ] **Step 5: Empty State에 FADE_UP 적용**

기존 관심 0명 Empty State div(`background:"rgba(255,255,255,0.04)"`)를 `motion.div`로 교체.
(`CharacterGridCard`는 Step 2&3에서 이미 정의됨 — 여기서 재정의 금지):
```tsx
<motion.div
  initial={FADE_UP.initial}
  animate={FADE_UP.animate}
  transition={{ duration: 0.3 }}
  style={{ background:"rgba(255,255,255,0.04)", borderRadius:"12px", padding:"24px", textAlign:"center" }}
>
  <div style={{ color:"rgba(255,255,255,0.4)", fontSize:"12px", marginBottom:"4px" }}>아직 관심 캐릭터가 없어요</div>
  <div style={{ color:"#0EA5E9", fontSize:"11px" }}>탐색하러 가기 →</div>
</motion.div>
```

- [ ] **Step 6: 빌드 확인**

```bash
cd Design_main_screen && npm run build 2>&1 | tail -5
```
Expected: `✓ built in`

- [ ] **Step 7: 커밋**

```bash
git add src/app/components/ProfileTabScreen.tsx
git commit -m "feat(ProfileTab): grid stagger + avatar shimmer + empty state animation"
```

---

## 최종 검증

- [ ] **전체 빌드 통과 확인**

```bash
npm run build 2>&1 | tail -10
```
Expected: `✓ built in` (경고는 있어도 되지만 오류는 없어야 함)

- [ ] **개발 서버 실행 후 수동 확인**

```bash
npm run dev
```

체크리스트:
- [ ] BottomNav 탭 전환 시 아이콘이 bounce하고 dot이 슬라이딩 이동
- [ ] 캐릭터 카드 탭 시 scale 축소 반응
- [ ] 홈 진입 시 카드들이 순서대로 올라옴
- [ ] 캐릭터 프로필 열 때 아래에서 슬라이드업, 닫을 때 슬라이드다운
- [ ] 관심 버튼 탭 시 scale bounce
- [ ] Explore 검색창 탭 시 오버레이가 fade+scale로 나타남
- [ ] Chat 탭 대화 목록이 순서대로 등장
- [ ] Profile 탭 관심 캐릭터 그리드가 순서대로 팝업
- [ ] 아바타 이미지 로딩 전 shimmer 표시
