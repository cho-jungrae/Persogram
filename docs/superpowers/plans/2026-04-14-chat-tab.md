# Chat 탭 구현 계획

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Chat 탭 UI 구현 — AI 상태 업데이트 스트립 + 대화 목록 + BottomNav 연결

**Architecture:** HomeScreen의 `activeNav` 상태를 활용해 Chat 탭 선택 시 `ChatTabScreen`을 렌더링한다. ChatTabScreen은 즐겨찾기 캐릭터 스트립(상단)과 대화 목록(하단)으로 구성되며, 스트립 터치 시 ProfileScreen, 목록 터치 시 ChatScreen으로 이동한다.

**Tech Stack:** React 18, TypeScript, Tailwind CSS 4, lucide-react

---

## 파일 구조

- **신규 생성:** `src/app/components/ChatTabScreen.tsx` — Chat 탭 전체 화면
- **수정:** `src/app/components/HomeScreen.tsx` — Chat 탭 라우팅 추가

---

## Chunk 1: ChatTabScreen 컴포넌트

### Task 1: ChatTabScreen 기본 구조 생성

**Files:**
- Create: `Design_main_screen/src/app/components/ChatTabScreen.tsx`

- [ ] **Step 1: ChatTabScreen.tsx 파일 생성**

```tsx
import { Character } from "./ProfileScreen";

// 목업 데이터 타입
interface FavoriteCharacter {
  character: Character;
  hasUpdate: boolean;
  isOnline: boolean;
}

interface ConversationItem {
  id: string;
  character: Character;
  lastMessage: string;
  lastMessageTime: string;
  unreadCount: number;
  isOnline: boolean;
}

interface ChatTabScreenProps {
  onSelectCharacter: (character: Character) => void; // → ProfileScreen
  onStartChat: (character: Character) => void;       // → ChatScreen
}
```

- [ ] **Step 2: 목업 데이터 정의**

즐겨찾기 캐릭터 2명 (업데이트 있음), 2명 (업데이트 없음):
```tsx
// characters 배열에서 import 불가 → HomeScreen에서 prop으로 전달
// 단순화: ChatTabScreen 내부에 목업 데이터 하드코딩
const mockFavorites: FavoriteCharacter[] = [
  {
    character: { id: 1, name: "동경 유학생 진우", accentColor: "#38BDF8", ... },
    hasUpdate: true,
    isOnline: true,
  },
  {
    character: { id: 3, name: "하나 사쿠라", accentColor: "#22D3EE", ... },
    hasUpdate: true,
    isOnline: false,
  },
  {
    character: { id: 4, name: "린 섀도우블레이드", accentColor: "#06B6D4", ... },
    hasUpdate: false,
    isOnline: false,
  },
  {
    character: { id: 2, name: "비서김비서", accentColor: "#0EA5E9", ... },
    hasUpdate: false,
    isOnline: false,
  },
];

const mockConversations: ConversationItem[] = [
  {
    id: "1",
    character: mockFavorites[0].character,
    lastMessage: "신주쿠에 같이 가고 싶다~ 🌸",
    lastMessageTime: "오후 2:14",
    unreadCount: 2,
    isOnline: true,
  },
  {
    id: "2",
    character: mockFavorites[1].character,
    lastMessage: "また話しかけてね！",
    lastMessageTime: "어제",
    unreadCount: 0,
    isOnline: false,
  },
  {
    id: "3",
    character: mockFavorites[2].character,
    lastMessage: "...생각해볼게.",
    lastMessageTime: "3일 전",
    unreadCount: 0,
    isOnline: false,
  },
];
```

- [ ] **Step 3: FavoritesStrip 렌더링 구현**

```tsx
// 상단 AI 상태 업데이트 스트립
function FavoritesStrip({
  favorites,
  onSelect,
}: {
  favorites: FavoriteCharacter[];
  onSelect: (character: Character) => void;
}) {
  return (
    <div style={{ padding: "0 16px 12px" }}>
      <div style={{ color: "rgba(255,255,255,0.4)", fontSize: "9px", marginBottom: "8px", letterSpacing: "0.5px" }}>
        즐겨찾기
      </div>
      <div style={{ display: "flex", gap: "12px", overflowX: "auto", paddingBottom: "4px", scrollbarWidth: "none" }}>
        {favorites.map((fav) => (
          <button
            key={fav.character.id}
            onClick={() => onSelect(fav.character)}
            style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: "4px", flexShrink: 0, background: "none", border: "none", cursor: "pointer", opacity: fav.hasUpdate ? 1 : 0.45 }}
          >
            <div style={{ position: "relative" }}>
              {/* 그라디언트 링 (업데이트 있을 때) */}
              <div style={{
                width: "50px", height: "50px", borderRadius: "50%",
                background: fav.hasUpdate
                  ? "linear-gradient(135deg, #F59E0B, #EF4444)"
                  : "rgba(255,255,255,0.2)",
                padding: "2px",
              }}>
                <div style={{
                  width: "100%", height: "100%", borderRadius: "50%",
                  background: "#0B1526",
                  padding: "2px",
                }}>
                  <div style={{
                    width: "100%", height: "100%", borderRadius: "50%",
                    background: `linear-gradient(135deg, ${fav.character.accentColor}, #0369A1)`,
                  }} />
                </div>
              </div>
              {/* 온라인 뱃지 */}
              {fav.isOnline && (
                <div style={{
                  position: "absolute", bottom: "1px", right: "1px",
                  width: "13px", height: "13px", borderRadius: "50%",
                  background: "#4ADE80", border: "2px solid #0B1526",
                }} />
              )}
            </div>
            <span style={{
              color: fav.hasUpdate ? "rgba(255,255,255,0.85)" : "rgba(255,255,255,0.5)",
              fontSize: "8px", fontWeight: fav.hasUpdate ? 600 : 400,
              maxWidth: "50px", textAlign: "center",
              overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap",
            }}>
              {fav.character.name.split(" ").slice(-1)[0]}
            </span>
          </button>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 4: ConversationList 렌더링 구현**

```tsx
function ConversationList({
  conversations,
  onSelect,
}: {
  conversations: ConversationItem[];
  onSelect: (character: Character) => void;
}) {
  return (
    <div style={{ flex: 1, overflowY: "auto" }}>
      {conversations.map((conv) => (
        <button
          key={conv.id}
          onClick={() => onSelect(conv.character)}
          style={{
            display: "flex", alignItems: "center", gap: "10px",
            padding: "10px 16px", width: "100%",
            borderBottom: "1px solid rgba(255,255,255,0.05)",
            background: "none", border: "none", cursor: "pointer", textAlign: "left",
            borderBottomWidth: "1px", borderBottomStyle: "solid", borderBottomColor: "rgba(255,255,255,0.05)",
          }}
        >
          {/* 아바타 + 온라인 점 */}
          <div style={{ position: "relative", flexShrink: 0 }}>
            <div style={{
              width: "42px", height: "42px", borderRadius: "50%",
              background: `linear-gradient(135deg, ${conv.character.accentColor}, #0369A1)`,
            }} />
            {conv.isOnline && (
              <div style={{
                position: "absolute", bottom: 0, right: 0,
                width: "11px", height: "11px", borderRadius: "50%",
                background: "#4ADE80", border: "2px solid #0B1526",
              }} />
            )}
          </div>
          {/* 텍스트 */}
          <div style={{ flex: 1, minWidth: 0 }}>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: "2px" }}>
              <span style={{ color: "white", fontSize: "12px", fontWeight: conv.unreadCount > 0 ? 700 : 600 }}>
                {conv.character.name}
              </span>
              <span style={{ color: "rgba(255,255,255,0.35)", fontSize: "9px" }}>
                {conv.lastMessageTime}
              </span>
            </div>
            <span style={{
              color: conv.unreadCount > 0 ? "rgba(255,255,255,0.6)" : "rgba(255,255,255,0.4)",
              fontSize: "11px",
              overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap", display: "block",
            }}>
              {conv.lastMessage}
            </span>
          </div>
          {/* 읽지 않음 뱃지 */}
          {conv.unreadCount > 0 && (
            <div style={{
              width: "18px", height: "18px", borderRadius: "50%",
              background: "#0EA5E9", display: "flex", alignItems: "center", justifyContent: "center",
              flexShrink: 0,
            }}>
              <span style={{ color: "white", fontSize: "9px", fontWeight: 700 }}>{conv.unreadCount}</span>
            </div>
          )}
        </button>
      ))}
    </div>
  );
}
```

- [ ] **Step 5: ChatTabScreen 메인 컴포넌트 완성**

```tsx
export function ChatTabScreen({ onSelectCharacter, onStartChat }: ChatTabScreenProps) {
  return (
    <div style={{
      position: "fixed", inset: 0, background: "#0B1526",
      display: "flex", flexDirection: "column", zIndex: 20,
    }}>
      {/* 헤더 */}
      <div style={{
        padding: "52px 16px 12px",
        display: "flex", alignItems: "center", justifyContent: "space-between",
        flexShrink: 0,
      }}>
        <span style={{ color: "white", fontSize: "18px", fontWeight: 800 }}>채팅</span>
        <button
          onClick={() => {/* Home 탭으로 이동 — onGoHome prop 추가 예정 */}}
          style={{
            width: "30px", height: "30px", borderRadius: "8px",
            background: "rgba(255,255,255,0.08)",
            display: "flex", alignItems: "center", justifyContent: "center",
            border: "none", cursor: "pointer", color: "rgba(255,255,255,0.7)", fontSize: "16px",
          }}
        >
          ✏️
        </button>
      </div>

      {/* AI 상태 스트립 */}
      <FavoritesStrip favorites={mockFavorites} onSelect={onSelectCharacter} />

      {/* 구분선 */}
      <div style={{ height: "1px", background: "rgba(255,255,255,0.07)", margin: "0 16px", flexShrink: 0 }} />

      {/* 대화 목록 */}
      <ConversationList conversations={mockConversations} onSelect={onStartChat} />
    </div>
  );
}
```

- [ ] **Step 6: 파일 저장 확인**

---

### Task 2: HomeScreen에 Chat 탭 연결

**Files:**
- Modify: `Design_main_screen/src/app/components/HomeScreen.tsx`

- [ ] **Step 1: ChatTabScreen import 추가**

```tsx
import { ChatTabScreen } from "./ChatTabScreen";
```

- [ ] **Step 2: Chat 탭 렌더링 조건 추가**

`activeNav === "chat"` 일 때 ChatTabScreen을 표시:

```tsx
{/* ── CHAT TAB ── */}
{activeNav === "chat" && !chatCharacter && !selectedCharacter && (
  <ChatTabScreen
    onSelectCharacter={(char) => setSelectedCharacter(char)}
    onStartChat={(char) => setChatCharacter(char)}
  />
)}
```

위치: ProfileScreen/ChatScreen 조건 렌더링 블록 바로 아래, home content 블록 위.

- [ ] **Step 3: Home 콘텐츠 렌더링 조건 수정**

기존:
```tsx
{!chatCharacter && !selectedCharacter && <>
```

변경:
```tsx
{activeNav === "home" && !chatCharacter && !selectedCharacter && <>
```

- [ ] **Step 4: BottomNav 가시성 조건 확인**

Chat 탭 활성 상태에서 BottomNav가 표시되어야 함.
기존 조건 `!selectedCharacter && !chatCharacter` 유지.
ChatTabScreen 위에 BottomNav가 올라오게 z-index 확인.

ChatTabScreen의 `zIndex: 20`이 BottomNav의 `z-40`보다 낮으므로 BottomNav는 정상 표시됨.

- [ ] **Step 5: 개발 서버에서 동작 확인**

```bash
cd "d:/[00].ANTI.G/Persogram/Design_main_screen"
npm run dev
```

확인 사항:
- BottomNav에서 Chat 탭 터치 시 어두운 Chat 탭 화면 표시
- 즐겨찾기 스트립: 진우/하나 → 그라디언트 링, 린/비서 → 반투명
- 즐겨찾기 아이콘 터치 → ProfileScreen 이동
- 대화 목록 항목 터치 → ChatScreen 이동
- BottomNav에서 Home 탭으로 돌아오면 기존 피드 표시

---

## 완료 기준

- [ ] Chat 탭 탭 전환 시 ChatTabScreen 표시
- [ ] AI 상태 스트립 (즐겨찾기 가로 스크롤) 렌더링
- [ ] 업데이트 있는 캐릭터: 그라디언트 링, 이름 강조
- [ ] 업데이트 없는 캐릭터: 회색, 반투명
- [ ] 즐겨찾기 아이콘 터치 → ProfileScreen 이동
- [ ] 대화 목록 터치 → ChatScreen 이동
- [ ] BottomNav 정상 표시 (Chat 탭 위)
